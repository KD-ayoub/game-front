export type PaddleType = {
    x: number,
    y: number,
    gap: number,
    speed: number,
    color: string,
    width: number,
    height: number,
    tableWidth: number,
    tableHeight: number
}
